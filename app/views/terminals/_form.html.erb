<%= form_with(model: terminal) do |form| %>
  <% if terminal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(terminal.errors.count, "error") %> prohibited this terminal from being saved:</h2>

      <ul>
        <% terminal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, class: 'form-label'%>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div>
    <%= form.label :code, class: 'form-label'%>
    <%= form.text_field :code, class: 'form-control' %>
  </div>

  <div>
    <%= form.label :latitude, class: 'form-label'%>
    <%= form.text_field :latitude, class: 'form-control' %>
  </div>

  <div>
    <%= form.label :longitude, class: 'form-label'%>
    <%= form.text_field :longitude, class: 'form-control' %>
  </div>

  <div>
    <%= form.label :city_id, class: 'form-label'%>
    <%= form.collection_select(:city_id, City.all, :id, :name, {:prompt=>true}, {:class=>'form-control'}) %>
  </div>

  <div>
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
